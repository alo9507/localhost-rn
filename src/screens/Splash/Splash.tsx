import React, { useEffect, useState } from "react";
import { Text } from "react-native";

const Splash = (props) => {

    return (
        <>
            <Text>Splash</Text>
        </>
    );
};

export default Splash;
