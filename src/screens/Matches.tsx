import React, { useEffect, useState } from "react";
import { Text } from "react-native";

const Matches = (props) => {

    return (
        <>
            <Text>Matches</Text>
        </>
    );
};

export default Matches;
